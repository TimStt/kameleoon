import UIBackButton from "@shared/ui/ui-back-button";
import Layout from "@widgets/layout/layout";

export const Finalize = () => {
  return (
    <Layout title="Finalize" subtitle="Spring promotion">
      <UIBackButton className="left bottom" />
    </Layout>
  );
};
